---
name: crossframe-promax
description: Use only when the user explicitly names crossframe-promax, CrossFrame ProMax, $crossframe-promax, or /crossframe-promax; never infer activation from generic requests for depth or completeness.
---

# CrossFrame ProMax adapter for Windsurf

Read `skills/crossframe-promax/SKILL.md` completely and execute that canonical v8-only, exact-name only runtime. Resolve every relative resource path from `skills/crossframe-promax/`.

If both Max and ProMax are named, ProMax wins. Routing invariant: generic maximality remains Max; suite never auto-upgrades to ProMax. ProMax uses its own audit and has no review chain. An active ProMax run never falls back to Max.
