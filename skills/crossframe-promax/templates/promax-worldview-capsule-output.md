# Worldview capsule 生成合同

产物：`promax-worldview-capsule.locked.md`
生成阶段：`P2`
冻结输入：已验证的 source snapshot、完整 read events、v8 全源、registry、contracts、routes

该胶囊是完成全源连续读取后的运行索引，不是原文摘要替代品。只有 3,863 个段落与 117 张表的 read-event 全覆盖已经绑定当前 snapshot，才可封存。

## 必须包含的正文结构

1. `# ProMax v8 世界观运行胶囊`
2. `## 权威边界`：写明本轮唯一源快照、锚点有效域、registry/contracts/routes 的职责，以及索引不能替代原文。
3. `## 对象与存在论约束`：用连续中文解释对象如何被划界、何时只能作为候选、何时不得因命名而实体化。
4. `## 尺度、圈层与双通道`：解释尺度变换、圈层候选、物质通道与体验—意义通道之间的约束，不把相邻概念合并成常识词义。
5. `## 事件、状态、机制与路径`：区分观察、报告、推断、反事实，以及机制、路径、时钟、停止点和结果写回。
6. `## 证据、判断与授权`：分开事实置信、结构判断、预测、规范选择与现实授权；写明证据不足时如何形成条件分支。
7. `## 保护与治理边界`：说明行动上限、保护门、撤回条件和不可逆行动的独立授权要求。
8. `## 本轮读取导航`：列出与当前请求最相关的 route IDs、源锚点范围和必须同读的概念群，但不得声称其它概念因此免检。

## 语义要求

- 每一节都必须形成完整论述，说明“定义—关系—限制—对本轮运行的影响”，不能只是术语列表。
- 引用概念时使用 registry 的精确 ID、权威中文名和定义；引用正文或表格时只使用当前 snapshot 的 `V8-P####`、`V8-T###` 锚点。
- 明确区分 v8 权威内容与本运行的对象化选择。运行胶囊不得创造 canonical concept。
- 不写未被源支持的理论补充，不引用其它 skill 作为知识来源，不披露私密推理过程。
- 末尾写出胶囊不能证明的事项，以及 P3 必须重新冻结的对象事实与未知项。

## 封存

以严格 UTF-8 写入完整 Markdown，再把实际字节散列登记到 P2 phase event。任何源绑定、read coverage 或正文改变都使旧胶囊及其下游失效。
